{
    "$schema": "https://schema.up.railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "streamlit run app.py --server.address 0.0.0.0 --server.port $PORT --server.fileWatcherType none --browser.gatherUsageStats false --client.showErrorDetails true --client.toolbarMode minimal --server.debug true"
    },
    "build": {
        "installCommand": "pip install -r requirements.txt"
    }
}
